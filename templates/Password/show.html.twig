{% extends 'base.html.twig' %}

{% block title %}Password{% endblock %}

{% block body %}
    <h1>Password</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ password.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ password.name }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ password.url }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ password.password }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('password_index') }}">back to list</a>

    <a href="{{ path('password_edit', {'id': password.id}) }}">edit</a>

    {{ include('password/_delete_form.html.twig') }}
{% endblock %}
