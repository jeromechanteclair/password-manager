{% extends 'base.html.twig' %}

{% block title %}User index{% endblock %}
{% block navigation %}
<header>
  <nav>
    {% if app.user %}
    <div class="nav-left">
      <div class="dropdown"><a>Bonjour <b>{{ user.username }}</b></a>
        <div>
        <a href="{{ path('user_show',{'id':user.id}) }}">Mes informations</a>
        <a href="{{ path('user_edit',{'id':user.id}) }}">Editer mon profil</a>
        <a href="/logout">Me déconnecter</a>
      </div>
    </div>
    </div>
    {% endif %}
    <div class="nav-right">
      <a href="{{ path('create_password') }}"><button>createPassword</button></a>
    </div>
  </nav>
</header>
{% endblock %}
